## Environment Variables
- `DATABASE_PATH`: Path to SQLite database (e.g., `/app/data/database.db`) or `DATABASE_URL` for PostgreSQL.
- `FLASK_SECRET_KEY`: Secure key for Flask sessions (e.g., 32-character random string).
- `ADMIN_PASSWORD`: Password for admin panel access (e.g., strong password).